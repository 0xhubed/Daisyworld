/**
 * Daisyworld Simulation
 * Main entry point for the Daisyworld model
 */

// Import and export model classes
const { DaisyworldModel, Planet, Daisy } = require('./src/model');

// Export model classes
module.exports = {
  DaisyworldModel,
  Planet,
  Daisy
};